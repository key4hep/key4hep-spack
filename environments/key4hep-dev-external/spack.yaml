spack:
  packages:
    all:
      variants:
        - build_type=RelWithDebInfo
        - 'cxxstd=20'
      target: [x86_64, aarch64]

    boost:
      require: +atomic+chrono+context+date_time+exception+fiber+filesystem+graph+iostreams+locale+log+math+multithreaded+program_options+python+random+regex+serialization+shared+system+test+thread+timer+wave
    # Keep the possibility to build Gaudi 38
    fmt:
      require: "@:10"

  concretizer:
    targets:
      host_compatible: true

  view: false
  include:
  - ../key4hep-common/packages.yaml

  specs:
  - key4hep-external-stack
