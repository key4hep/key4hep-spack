# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  include:
  - key4hep-packages.yaml
  - compilers.yaml
  packages:
    all:
      compiler: [clang@12.0.0]
  view: false
  repos:
  - /home/plfernan/workspace/key4hep-spack
