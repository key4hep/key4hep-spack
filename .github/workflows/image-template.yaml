name: Publish key4hep-externals image
description: |
  Loads a key4hep-dev-external stack from spack cache.
  This workflow builds the image and pushes it to the registry.

on:
  workflow_call:
    inputs:
      os:
        required: true
        type: string

env:
  REGISTRY: ghcr.io

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  build-linux-amd64:
    uses: ./.github/workflows/docker-build-push-template.yaml
    with:
      os: ${{ inputs.os }}
      runner: ubuntu-latest
      platform: linux/amd64
      registry: ${{ env.REGISTRY }}

  build-linux-arm64:
    uses: ./.github/workflows/docker-build-push-template.yaml
    with:
      os: ${{ inputs.os }}
      runner: ubuntu-24.04-arm
      platform: linux/arm64
      registry: ${{ env.REGISTRY }}

  publish-multiarch:
    needs:
      - build-linux-amd64
      - build-linux-arm64
    runs-on: ubuntu-latest
    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker Meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/key4hep-externals-${{ inputs.os }}
      - name: Create manifest
        id: build
        uses: int128/docker-manifest-create-action@v2
        with:
          index-annotations: ${{ steps.meta.outputs.labels }}
          tags: ${{ steps.meta.outputs.tags }}
          sources: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/key4hep-externals-${{ inputs.os }}@${{ needs.build-linux-amd64.outputs.digest }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/key4hep-externals-${{ inputs.os }}@${{ needs.build-linux-arm64.outputs.digest }}
